sudo: required
services:
  - docker

before_install:
  - docker build -t verticalambition/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run verticalambition/react-test npm test -- --coverage

after_success:
  - docker build -t verticalambition/multi-client ./client
  - docker build -t verticalambition/multi-nginx ./nginx
  - docker build -t verticalambition/multi-server ./server
  - docker build -t verticalambition/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push verticalambition/multi-client
  - docker push verticalambition/multi-nginx
  - docker push verticalambition/multi-server
  - docker push verticalambition/multi-worker
